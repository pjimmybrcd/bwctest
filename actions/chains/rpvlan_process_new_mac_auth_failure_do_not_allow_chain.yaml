---
  chain:
    -
      name: "rpvlan_mac_auth_failure_do_not_allow_database"
      ref: "campus_ztp.rpvlan_mac_auth_failure_do_not_allow_database"
      parameters:
        device: "{{device}}"
        mac: "{{mac}}"
        port: "{{port}}"
